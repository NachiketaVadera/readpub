# readpub_reader example

Reads *The Lantern Keeper*, an original sample book bundled in `assets/`, with
paged and scrolled flows, themes, text size, contents, bookmarks, search and a
position slider. Rebuild the book with `python3 tool/build_sample.py`.

```sh
flutter run
flutter test integration_test/reader_test.dart -d <device>
```
